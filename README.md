# React-Webpack-Gulp

--

A simple react webpack gulp demo app.

+ Running

    ```bash
    # init dev modules
    ./init.sh 	
    # build source
    gulp 		
    # open login.html in browser
    open login.html
    ```
